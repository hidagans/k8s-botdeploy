apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: code-${BOT_ID}-pvc
  namespace: bots
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: code-server-storage
  resources:
    requests:
      storage: 1Gi